const fs = require('fs');

function node(value) {
    return {
        value: valye,
        next
    };
};

function parse(path) {
    const raw = fs.readFileSync(path,'utf8')
    const data = raw.trim().split(' ');
    return data;
};
module.exports = parse;
